[package]
name = "reliquary-web"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Web frontend for Reliquary — browse and edit physical media collections from any device"
keywords = ["media", "archive", "web"]
categories = ["web-programming"]

[lints]
workspace = true

[dependencies]
reliquary = { version = "0.1.0", path = "../reliquary" }

# REMOVE when reliquary is used in code (skeleton dep)
[package.metadata.cargo-machete]
ignored = ["reliquary"]
